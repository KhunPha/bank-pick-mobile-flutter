import 'package:flutter/material.dart';

class MyCardTab extends StatelessWidget {
  const MyCardTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}